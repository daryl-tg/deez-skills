---
description: Commit the currently staged changes.
---

Read `~/.claude/skills/commit/SKILL.md` and follow it.

Commit only what is already staged. If nothing is staged, say so and stop.

$ARGUMENTS
