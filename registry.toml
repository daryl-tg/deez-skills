# The index. Every skill, command, and subagent this hub owns is listed here,
# and nothing is linked into a runtime unless it appears below.
#
# Adding an entry by hand is fine, but `bin/new` and `bin/adopt` do it for you
# and regenerate the README. `bin/doctor` fails if this file and the folders on
# disk disagree.

[meta]
version = 1
default_profile = "full"

# Categories are the unit profiles select on, so keep them coarse.
[categories]
core     = "Always-on essentials"
om-chat  = "OM Chat feature delivery"
kiyotaka = "Kiyotaka / orange stack"
workflow = "General workflow"
writing  = "Writing and editing"
infra    = "Infrastructure and observability"

# A profile is what one machine installs. "*" means every category.
# Skill metadata costs ~5k tokens of context per session at ~70 skills, so a
# machine that does not need a stack should not carry it.
[profiles]
full = ["*"]
lean = ["core", "workflow", "writing"]

# What may be delegated, and what never may.
#
# Both runtimes run the full suite, so this no longer picks a runtime: the
# operator does that when they choose a CLI. `default_lane` records which lane
# implementation goes to within the current runtime.
#
# The four below it are not preferences. Design, review, verification, and every
# git mutation stay with the lead on either runtime, and the registry rejects
# any other value for them.
[routing]
default_lane  = "claude"
planning      = "claude"
review        = "claude"
verification  = "claude"
git_mutations = "claude"

# ---------------------------------------------------------------------------
# Every entry declares a `layer`, which decides how it may be invoked:
#
#   mode           the router. Sticky, explicitly routed to, Claude only.
#   principle      one rule. Cited by playbooks. Must be on BOTH runtimes so a
#                  handoff citation resolves on either side.
#   playbook-host  owns a playbooks/ directory. Claude only.
#   workflow       everything else. May stay model-invocable. The default.
#
# mode, principle, and playbook-host must ship disable-model-invocation: true.
# bin/doctor fails the build when a layer and its frontmatter disagree.
# ---------------------------------------------------------------------------

[skills]

# --- Verification substrate -------------------------------------------------
# The memory and context every other layer reads. Explicitly invoked, so both
# carry the flag even though `workflow` does not require it.

[skills.create-verification-skill]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.maintain-verification-skill]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

# --- Principles -------------------------------------------------------------
# One rule each. Cited by playbooks, never restated in them. Installed on BOTH
# runtimes so a handoff citing one resolves on the Codex side too.

[skills.principle-announce-the-linked-review]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-bind-assigned-ports]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-build-the-lever]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-delegate-implementation-review-stays-here]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-desktop-before-cloud]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-failing-test-first]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-feature-branch-isolation]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-finish-or-report]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-never-block-on-reversible-work]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-one-commit-lands]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-planning-docs-live-outside-the-repo]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-promote-to-the-main-worktree]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-prove-on-the-real-surface]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-rebase-pr-squash]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-separate-before-serializing-shared-state]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-todo-discipline]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-visual-approval-gates-delivery]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-boundary-discipline]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-encode-lessons-in-structure]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-exhaust-the-design-space]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-experience-first]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-fix-root-causes]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-foundational-thinking]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-guard-the-context-window]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-laziness-protocol]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-make-operations-idempotent]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-migrate-callers-then-delete-legacy-apis]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-minimize-reader-load]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-model-the-domain]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-outcome-oriented-execution]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-redesign-from-first-principles]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-sequence-verifiable-units]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-subtract-before-you-add]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

[skills.principle-type-system-discipline]
layer    = "principle"
category = "core"
runtimes = ["claude", "codex"]

# --- Router -----------------------------------------------------------------
# One router, Claude only. Codex ignores disable-model-invocation, so a mode
# skill there would fire on description matches. Its playbooks are plain files,
# readable by absolute path from either runtime without being installed.

[skills.clanker-mode]
layer    = "mode"
category = "core"
runtimes = ["claude", "codex"]

# --- Workflow skills --------------------------------------------------------
# Adopted from pstack where nothing local covered the ground, merged with the
# local counterpart where both had a stronger half. Installed on both runtimes:
# an executor needs the rules and the craft skills, just not the routing.

[skills.unslop]
layer    = "workflow"
category = "writing"
runtimes = ["claude", "codex"]

[skills.teach]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.why]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.recall]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.blast-radius]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.show-me-your-work]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.figure-it-out]
layer    = "workflow"
category = "workflow"
runtimes = ["claude", "codex"]

[skills.reflect]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.automate-me]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.design]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.test-first]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.review]
layer    = "workflow"
category = "core"
runtimes = ["claude", "codex"]

[skills.writing-docs]
layer    = "workflow"
category = "writing"
runtimes = ["claude", "codex"]

[commands]

[agents]

[agents.clanker-agent]
layer    = "workflow"
category = "core"
runtimes = ["claude"]

[skills.om-chat-design-system]
category = "om-chat"
runtimes = ["claude", "codex"]

[skills.om-super-reaction]
category = "om-chat"
runtimes = ["claude", "codex"]

[skills.codex-first]
category = "core"
runtimes = ["claude", "codex"]

[skills.herdr-codex-orchestration]
category = "core"
runtimes = ["claude", "codex"]

[skills.writing-simplified-technical-english]
category = "writing"
runtimes = ["claude", "codex"]

[skills.om-build]
category = "om-chat"
runtimes = ["claude", "codex"]

[skills.om-chat]
category = "om-chat"
runtimes = ["claude", "codex"]

[skills.verify-om-chat]
category = "om-chat"
runtimes = ["claude", "codex"]

[skills.llm-council]
category = "workflow"
runtimes = ["claude", "codex"]

[skills.writing-instructions]
category = "writing"
runtimes = ["claude", "codex"]
