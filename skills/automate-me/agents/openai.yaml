interface:
  display_name: "Automate Me"
  short_description: "Draft or refresh the operator's personal mode skill from rea"
# Explicitly invoked, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
