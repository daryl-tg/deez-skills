interface:
  display_name: "Design"
  short_description: "Settle the shape before writing code: ground, sketch competi"
# Explicitly invoked, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
