interface:
  display_name: "Om Build"
  short_description: "Build an OM Chat GUI from source and put it in front of the"
# Explicitly invoked, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
