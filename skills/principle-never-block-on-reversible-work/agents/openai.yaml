interface:
  display_name: "Never Block On Reversible Work"
  short_description: "Apply when tempted to ask permission for reversible work"
# Routed to explicitly, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
