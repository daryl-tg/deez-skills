interface:
  display_name: "Sequence Verifiable Units"
  short_description: "Apply to multi-step work and to how you stack commits"
# Routed to explicitly, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
