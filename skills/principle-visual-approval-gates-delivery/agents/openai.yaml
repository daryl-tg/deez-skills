interface:
  display_name: "Visual Approval Gates Delivery"
  short_description: "Apply before promoting, opening a review request, or announc"
# Routed to explicitly, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
