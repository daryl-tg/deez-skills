interface:
  display_name: "Review"
  short_description: "Review a diff from the code, with findings sorted into act-o"
# Explicitly invoked, never matched. The Codex counterpart of
# disable-model-invocation in the frontmatter.
policy:
  allow_implicit_invocation: false
