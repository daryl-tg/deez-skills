# Third-party skills this hub does NOT vendor. ~/.agents/skills is owned by the
# skill fetcher and its .skill-lock.json; this file records what should be
# present so a fresh machine is reproducible, and lets bin/doctor warn when the
# pins disagree. Nothing here is ever written by deez.
#
# Populated during migration — docs/migration.md carries the full list of 14.

[vendor.animate]
source   = "github:emilkowalski/skill"
path     = "skills/animate"
runtimes = ["claude"]

[vendor.agent-browser]
source   = "github:obra/agent-browser"
path     = "skills/agent-browser"
runtimes = ["claude", "codex"]

# Ships with the `om` CLI and self-updates through `.om-skill-manifest.json`.
# The hub must not own a copy: a vendored skill that is also checked in here
# goes stale the moment `om` updates, and then two versions disagree about the
# tool contract. Recorded so a fresh machine knows it should be present.
[vendor.om-chat]
source   = "om-cli"
path     = "claude/skills/om-chat"
runtimes = ["claude", "codex"]
note     = "Synced by the om CLI. Never hand-edit, never vendor into skills/."
